name: Test Docker image

on:
  pull_request:
    paths-ignore:
      - 'deploy/charts/**'
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: pr-${{ github.event.pull_request.number || github.ref }}-docker-test
  cancel-in-progress: true

permissions:
  contents: read
  security-events: write

jobs:
  test:
    uses: OmniTrustILM/.github/.github/workflows/containers-test.yml@main
    with:
      image: ilm/cbom-repository
      cache-scope: cbom-repository
      build-args: |
        VERSION=${{ github.ref_name }}
        APK_UPGRADE_STAMP=${{ github.run_id }}-${{ github.run_attempt }}
